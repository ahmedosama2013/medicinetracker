.env
.env.local
supabase/.env
supabase/.temp/
.supabase/
node_modules/
.DS_Store

# Not js/config.js. It is committed on purpose -- the anon key and the VAPID
# public key are meant to be public, and the deployed GitHub Pages site needs
# them. See docs/setup.md.

# Credentials that are NOT safe to commit. The Google OAuth client secret now
# lives outside the repository (~/.medicine-tracker/), which is the real
# protection; this line is a second one in case a copy reappears here.
custom.txt
*.secret
